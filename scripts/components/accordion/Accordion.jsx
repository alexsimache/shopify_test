import React from "react"
import ReactDom from "react-dom"

const Accordion = () => {
  return <div>React JS Accordion</div>
}

const root = document.getElementById('react-accordion');
ReactDom.render(<Accordion />, root);
